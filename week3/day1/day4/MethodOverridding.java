package week3.day4;

import org.openqa.selenium.chrome.ChromeDriver;

public class MethodOverridding  extends Overloading{
	
	//final is a keyword in Java that is used to declare constants, methods, and classes that cannot be changed or overridden.
	//final method cannot be overridden
	
	
	public void add(int num) {
		
		
		ChromeDriver driver = new ChromeDriver();
		driver.quit();
		
		
		String str="";
		if (str.equals(str)) {
			
			
			
		}

	}

	public void add(int num, String name) {

	}

	public void add(boolean flag) {

	}


}
